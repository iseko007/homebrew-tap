# Homebrew Tap

Homebrew casks for [MAI Second Brain](https://maicontext.com).

## Install

```
brew install --cask iseko007/tap/mai-second-brain
```

The app keeps itself up to date after that (Sparkle), so `brew upgrade` is
never required — though `brew upgrade --cask mai-second-brain` works too.
